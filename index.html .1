<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UniShell | عصر البرمجة الهجينة</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans+Arabic:wght@300;400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'IBM Plex Sans Arabic', sans-serif; background: #0f172a; overflow-x: hidden; }
        .glass { background: rgba(30, 41, 59, 0.7); backdrop-filter: blur(12px); border: 1px solid rgba(255, 255, 255, 0.1); }
        .glow-text { text-shadow: 0 0 20px rgba(59, 130, 246, 0.5); }
        .hero-gradient { background: radial-gradient(circle at center, #1e293b 0%, #0f172a 100%); }
        .animate-float { animation: float 6s ease-in-out infinite; }
        @keyframes float { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-20px); } }
    </style>
</head>
<body class="hero-gradient text-slate-200">

    <div class="fixed top-0 left-1/2 -translate-x-1/2 w-full h-full -z-10 opacity-30">
        <div class="absolute top-[-10%] left-[-10%] w-[50%] h-[50%] bg-blue-600 rounded-full blur-[120px]"></div>
        <div class="absolute bottom-[-10%] right-[-10%] w-[40%] h-[40%] bg-purple-600 rounded-full blur-[120px]"></div>
    </div>

    <main class="container mx-auto px-6 min-h-screen flex flex-col items-center justify-center py-20">
        
        <div class="animate-float text-center mb-12">
            <div class="inline-block px-4 py-1 rounded-full border border-blue-500/30 bg-blue-500/10 text-blue-400 text-sm mb-6">
                إصدار Alpha 1.0.4 - متاح الآن
            </div>
            <h1 class="text-7xl md:text-8xl font-black tracking-tighter text-white mb-4 glow-text">
                Uni<span class="text-blue-500">Shell</span>
            </h1>
            <p class="text-xl md:text-2xl text-slate-400 max-w-2xl mx-auto leading-relaxed">
                اللغة الأولى التي تفهم العتاد، تحمي البيئة، وتعمل على كل شيء.
            </p>
        </div>

        <div class="glass p-10 rounded-[2.5rem] shadow-2xl text-center mb-16 relative overflow-hidden group">
            <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-transparent via-blue-500 to-transparent"></div>
            <h2 class="text-blue-400 uppercase tracking-[0.2em] text-xs font-bold mb-6">المقاعد المجانية المتبقية للمؤسسين</h2>
            <div id="counter" class="text-8xl font-mono font-black text-white transition-all duration-500">
                10,000
            </div>
            <div class="mt-6 flex items-center justify-center gap-2">
                <span class="w-3 h-3 bg-green-500 rounded-full animate-ping"></span>
                <p class="text-slate-400 text-sm">تحديث مباشر من خادم النواة</p>
            </div>
        </div>

        <div class="flex flex-col items-center gap-6">
            <button onclick="triggerDownload()" class="relative group">
                <div class="absolute -inset-1 bg-gradient-to-r from-blue-600 to-cyan-500 rounded-2xl blur opacity-60 group-hover:opacity-100 transition duration-1000 group-hover:duration-200"></div>
                <div class="relative px-12 py-6 bg-slate-900 rounded-2xl flex items-center gap-4 border border-white/10 group-hover:border-white/20 transition-all">
                    <span id="osIcon" class="text-3xl">💻</span>
                    <div class="text-right">
                        <div class="text-[10px] text-blue-400 uppercase font-bold tracking-widest">تنزيل لـ</div>
                        <div id="osLabel" class="text-2xl font-bold">نظام التشغيل</div>
                    </div>
                </div>
            </button>
            <p class="text-slate-500 text-sm font-light">بموافقتك على التحميل، أنت تقبل <a href="#" class="underline hover:text-blue-400">شروط ترخيص الـ 10k</a></p>
        </div>
    </main>

    <script>
        // منطق التعرف على الجهاز (Device Identification)
        const getOS = () => {
            const platform = navigator.userAgent.toLowerCase();
            if (platform.includes('win')) return { name: 'Windows', icon: '🪟' };
            if (platform.includes('mac')) return { name: 'macOS', icon: '🍎' };
            if (platform.includes('linux')) return { name: 'Linux', icon: '🐧' };
            return { name: 'نظامك', icon: '🚀' };
        };

        const osInfo = getOS();
        document.getElementById('osLabel').innerText = osInfo.name;
        document.getElementById('osIcon').innerText = osInfo.icon;

        // محاكاة حية للعداد تعكس "نشاط النشر"
        let currentSeats = 10000;
        const updateCounter = () => {
            if (currentSeats > 9640) {
                currentSeats -= Math.floor(Math.random() * 5);
                document.getElementById('counter').innerText = currentSeats.toLocaleString();
                // تأثير نبضي عند التغيير
                document.getElementById('counter').classList.add('scale-105', 'text-blue-400');
                setTimeout(() => {
                    document.getElementById('counter').classList.remove('scale-105', 'text-blue-400');
                }, 200);
            }
        };
        setInterval(updateCounter, 4000);

        function triggerDownload() {
            alert(`بدأ سحب النواة لـ ${osInfo.name}. تم حجز مقعدك بنجاح!`);
        }
    </script>
</body>
</html>
